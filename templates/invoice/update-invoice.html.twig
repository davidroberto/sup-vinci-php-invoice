{% extends 'base.html.twig' %}

{% block main %}

    <h1>Mettre à jour une facture</h1>

    <form method="POST">

        <label> Titre
            <input type="text" name="title" value="{{ invoice.title }}">
        </label>

        <label> Description
            <input type="text" name="description" value="{{ invoice.description }}">
        </label>

        <label> Price
            <input type="number" step="0.01" name="price" value="{{ invoice.price }}">
        </label>

        <button type="submit">Modifier la facture</button>

    </form>

{% endblock %}

