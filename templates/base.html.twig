<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="{{ path('list_invoices') }}">Voir les factures</a></li>
                <li><a href="{{ path('create_invoice') }}">Créer une facture</a></li>
            </ul>
        </nav>
    </header>

    {% for message in app.flashes('success') %}
        <div style="background-color: green" class="success">
            {{ message }}
        </div>
    {% endfor %}

    {% block main %}
    {% endblock main %}

    <footer></footer>
</body>
</html>